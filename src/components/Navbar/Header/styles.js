import { css } from '@emotion/react';

export const headerStyles = css`
  background-color: var(--primary-color);
  padding: 1rem 0;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 900;
`;
